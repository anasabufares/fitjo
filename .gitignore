# Installed by Netlify (and locally) for the function's dependency — never commit
node_modules/

# Generated by the optional local server (safety backup of data.js) — not for deploy
data.js.bak

# Local stand-in for the cloud gym store, written by tools/server.ps1 — dev only
gyms.store.json

# OS / editor cruft
.DS_Store
Thumbs.db
